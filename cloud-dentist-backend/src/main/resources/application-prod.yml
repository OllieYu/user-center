spring:
  # DataSource Config
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/could-dentist
    username: root
    password: root
  #session 失效时间
  session:
    timeout: 86400
